using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Numerics;
using System.Threading.Tasks;

namespace Caladan.Frontend.Helpers
{
    public static class StringExtensions
    {
        //public static double FromHexWei(this string input, int toUnit)
        //{
        //    if (input == "0x0")
        //        return 0;
        //    if (input.StartsWith("0x"))
        //        input = input.Remove(0, 2);

        //    if (!ulong.TryParse(input, NumberStyles.HexNumber, CultureInfo.InvariantCulture, out var result)) return 0;
        //    return result / (toUnit == 0 ? 1 : Math.Pow(10, toUnit));
        //}
    }
}